spring:
  datasource:
    url: jdbc:mysql://localhost:3306/defense_management?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.defensemanagement.entity
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com.example.defensemanagement.mapper: debug